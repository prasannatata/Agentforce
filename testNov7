anonymousApex: |
  try {
      System.debug('========= START: ELIGIBILITY APEX TEST =========');

      // ✅ Use a real Account Id from your QA org here
      String accId = '001XXXXXXXXXXXX';

      // ✅ Create a fake HttpResponse (so your method won't blow up on res)
      HttpResponse fakeRes = new HttpResponse();
      fakeRes.setBody('{"DemographicInfo": null}');
      fakeRes.setStatusCode(200);

      // ✅ Call your Apex method EXACTLY as it is in your class
      EligibilitySummaryInvocable.Response result =
          EligibilitySummaryInvocable.createInsuranceCoverageRecord(
              fakeRes,
              accId
          );

      System.debug('✅ METHOD OUTPUT: ' + result);

  } catch (Exception ex) {
      System.debug('❌ ERROR FOUND ================================');
      System.debug('Message: ' + ex.getMessage());
      System.debug('Line Number: ' + ex.getLineNumber());
      System.debug('Stack Trace: ' + ex.getStackTraceString());
      System.debug('==============================================');
  }
