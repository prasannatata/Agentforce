AND(
    NOT(ISBLANK(MRN__c)),
    OR(
        NOT(ISBLANK(LABS_Patient_First_Initial__c)),
        NOT(ISBLANK(LABS_Patient_Last_Name__c))
    )
)





AND(
   ISPICKVAL(Status__c, "In Review"),
   NOT($Profile.Name = "System Administrator")
)
